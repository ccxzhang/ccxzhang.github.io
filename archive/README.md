# Archive

This folder contains files from the previous Jekyll-based site that are no longer part of the active React/Vite build.

- `archive/jekyll/_layouts`, `_config.yml`, and `assets/`: old Jekyll theme and asset pipeline
- `archive/jekyll/content`: markdown and static source files preserved for reference
